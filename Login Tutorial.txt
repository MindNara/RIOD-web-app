1. เปิด xamp 
2. เปิด apache + mySQL ( ไม่ต้อง Edit )
3. เปิด PHPmyAdmin
	วิธีเข้า
	- พิมพ์ใน URL -> localhost
	- เลือกปุ่มบนขวาของจอ
4. สร้าง db ทางซ้าย ชื่อว่า register_db
4.1. นำ Code ด้านล่างไปใส่ในช่อง "SQL" ( Tab ที่ 2 )
CREATE TABLE user (
    id INT(11) NOT NULL AUTO_INCREMENT PRIMARY KEY,
    username varchar(100) NOT NULL,
    password varchar(100) NOT NULL
) ENGINE = InnoDB DEFAULT CHARSET = utf8;